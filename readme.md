# Sample Cordova with Gimmie Widget

Build step

- Install [Cordova](http://cordova.apache.org) to your machine
- Clone this project and use terminal to build the project with `cordovar build`
- Add platform and run
